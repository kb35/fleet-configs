name: Fleet Deployment

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up SSH key
        run: |
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > private_key
          chmod 600 private_key
          ssh -i private_key -o StrictHostKeyChecking=no keith@192.168.68.61 "echo 'SSH connection successful'"

      - name: Run Ansible Playbook
        run: |
          ssh -o ConnectTimeout=30 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -i private_key keith@192.168.68.61 "ANSIBLE_DEBUG=true ansible-playbook -vvv ~/ansible-playbooks/update_fleet.yml"
